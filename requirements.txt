# Gold Bot — Python Dependencies
# Install with: pip install -r requirements.txt

yfinance>=0.2.40        # Yahoo Finance market data
pandas>=2.0.0           # Data manipulation
numpy>=1.24.0           # Numerical computing (Signal 11 ML — logistic regression)
requests>=2.31.0        # HTTP requests (FRED API, NewsAPI)
python-dateutil>=2.8.2  # Date utilities
pytz>=2024.1            # Timezone support

# Optional (Signal 11 runs without these — pure numpy fallback is built-in)
# scikit-learn>=1.3.0   # If you want to extend ML beyond the built-in LR
